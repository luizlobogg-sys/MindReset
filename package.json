{
  "name": "mindreset",
  "version": "4.0.0",
  "private": true,
  "description": "MindReset - Digital wellbeing app",
  "scripts": {
    "sync": "npx cap sync android",
    "build-apk": "npx cap sync android && cd android && ./gradlew assembleDebug",
    "build-aab": "npx cap sync android && cd android && ./gradlew bundleRelease"
  },
  "dependencies": {
    "@capacitor/android": "7.4.0",
    "@capacitor/core": "7.4.0",
    "@capacitor-firebase/authentication": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "7.4.0",
    "@capacitor/assets": "^3.0.0"
  }
}
